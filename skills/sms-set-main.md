---
name: sms-set-main
description: Mark the current Claude session as the branch's main session (replacing any prior main). Use when the user wants to promote a session to be the canonical / lead session for the branch. Runs `sms set-main`.
---

Run via the Bash tool:

  sms set-main "$CLAUDE_SESSION_ID"

Report the result.
