# sms — Session Management System

Branch-scoped session management for Claude Code. See design spec in
`docs/superpowers/specs/2026-06-22-sms-branch-scoped-sessions-design.md`

This is a standalone Python tool. Install with `./install.sh`.
